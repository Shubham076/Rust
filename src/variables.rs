// variables are immutable by default
// variable naming convention in snake_case
// we can use mut to make them mutable

fn main(){
    let x = 10;
    let name  = "Shubham";
    let mut last_name = "Dogra";
    let cancelled = true;
    let sum = 0.3;
    let char = 'a';

//     declare multiple variables
    let (a, b, c) = (2, 3, 4);
}