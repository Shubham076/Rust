// variables are immutable by default
// we can use mut to make them mutable

fn main(){
    let x = 10;
    let name  = "Shubham";
    let mut last_name = "Dogra";
    let cancelled = true;
    let sum = 0.3;
    let char = 'a';
}